<?xml version="1.0" encoding="utf-8"?>
<!-- Transport glyphs, on the same 24dp grid AurixGlyph uses in Dart, so the
     island reads as the same icon set whichever side is drawing it. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24"
    android:tint="@color/island_text_primary">
    <path
        android:fillColor="#FFFFFFFF"
        android:pathData="M8,5.5 L18.5,12 L8,18.5 Z" />
</vector>
